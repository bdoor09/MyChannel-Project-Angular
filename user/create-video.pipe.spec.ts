import { CreateVideoPipe } from './create-video.pipe';

describe('CreateVideoPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateVideoPipe();
    expect(pipe).toBeTruthy();
  });
});
