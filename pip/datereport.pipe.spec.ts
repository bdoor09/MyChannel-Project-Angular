import { DatereportPipe } from './datereport.pipe';

describe('DatereportPipe', () => {
  it('create an instance', () => {
    const pipe = new DatereportPipe();
    expect(pipe).toBeTruthy();
  });
});
