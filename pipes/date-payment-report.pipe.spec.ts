import { DatePaymentReportPipe } from './date-payment-report.pipe';

describe('DatePaymentReportPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePaymentReportPipe();
    expect(pipe).toBeTruthy();
  });
});
