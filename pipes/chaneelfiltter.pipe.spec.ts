import { ChaneelfiltterPipe } from './chaneelfiltter.pipe';

describe('ChaneelfiltterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChaneelfiltterPipe();
    expect(pipe).toBeTruthy();
  });
});
