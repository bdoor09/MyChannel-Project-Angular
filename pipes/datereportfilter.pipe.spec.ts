import { DatereportfilterPipe } from './datereportfilter.pipe';

describe('DatereportfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DatereportfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
